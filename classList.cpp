#include <iostream>
#include <fstream>
#include <string>
#include <vector>

using namespace std;

int main () {
    string currentClass;
    ifstream classList;
    classList.open("listOfClasses", ios::in | ios::app);
    getline(classList, currentClass);

    while (currentClass != "end") {

    }
    



   return 0;

    return 0;
}